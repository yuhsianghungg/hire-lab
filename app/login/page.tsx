"use client";
import { useState } from "react";
export default function LoginPage() { const [register, setRegister] = useState(false); const [error, setError] = useState(""); const submit = async (e: React.FormEvent<HTMLFormElement>) => { e.preventDefault(); setError(""); const form = new FormData(e.currentTarget); const r = await fetch(`/api/member/${register ? "register" : "login"}`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(Object.fromEntries(form)) }); const data = await r.json(); if (!r.ok) setError(data.error); else location.href = "/member"; }; return <main className="member-auth"><a className="brand" href="/"><span>HL</span> hire Lab.</a><section><p className="eyebrow">MEMBER ACCOUNT</p><h1>{register ? "建立你的會員帳號" : "歡迎回來"}</h1><form onSubmit={submit}>{register && <input name="name" placeholder="姓名" required />}<input name="email" type="email" placeholder="Email" required /><input name="password" type="password" minLength={8} placeholder="密碼（至少 8 碼）" required />{error && <p className="form-error">{error}</p>}<button className="button button-dark">{register ? "註冊並登入" : "登入"}</button></form><button className="switch-auth" onClick={() => setRegister(!register)}>{register ? "已有帳號？登入" : "還沒有帳號？立即註冊"}</button></section></main>; }
