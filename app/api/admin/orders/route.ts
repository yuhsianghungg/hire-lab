import { env } from "cloudflare:workers";
import { currentMember, ensureMemberSchema } from "@/lib/member-auth";

const statuses = ["pending", "making", "shipped", "delivered", "cancelled"];
async function guard() { const member = await currentMember(); return member?.role === "admin" ? member : null; }
export async function GET() { await ensureMemberSchema(); if (!await guard()) return Response.json({ error: "無管理權限。" }, { status: 403 }); const { results } = await env.DB.prepare("SELECT o.id,o.order_number,o.item_summary,o.total,o.status,o.tracking_number,o.created_at,o.updated_at,u.name,u.email FROM orders o JOIN users u ON u.id=o.user_id ORDER BY o.created_at DESC").all(); return Response.json({ orders: results }); }
export async function POST(request: Request) { await ensureMemberSchema(); if (!await guard()) return Response.json({ error: "無管理權限。" }, { status: 403 }); const { email, itemSummary, total, status = "pending", trackingNumber = "" } = await request.json(); const user = await env.DB.prepare("SELECT id FROM users WHERE email=?").bind(String(email || "").trim().toLowerCase()).first<{ id: string }>(); if (!user) return Response.json({ error: "找不到此會員 Email。" }, { status: 400 }); if (!String(itemSummary || "").trim() || !Number.isInteger(Number(total)) || Number(total) < 0 || !statuses.includes(status)) return Response.json({ error: "請完整填寫正確的訂單資料。" }, { status: 400 }); const timestamp = new Date().toISOString(); const orderNumber = `HL-${timestamp.slice(2, 10).replaceAll("-", "")}-${crypto.randomUUID().slice(0, 5).toUpperCase()}`; await env.DB.prepare("INSERT INTO orders (id,order_number,user_id,item_summary,total,status,tracking_number,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(), orderNumber, user.id, String(itemSummary).trim(), Number(total), status, String(trackingNumber).trim() || null, timestamp, timestamp).run(); return Response.json({ ok: true }); }
export async function PATCH(request: Request) { await ensureMemberSchema(); if (!await guard()) return Response.json({ error: "無管理權限。" }, { status: 403 }); const { id, status, trackingNumber } = await request.json(); if (!id || !statuses.includes(status)) return Response.json({ error: "無效的訂單資料。" }, { status: 400 }); await env.DB.prepare("UPDATE orders SET status=?,tracking_number=?,updated_at=? WHERE id=?").bind(status, String(trackingNumber || "").trim() || null, new Date().toISOString(), id).run(); return Response.json({ ok: true }); }
