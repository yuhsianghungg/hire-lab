import { env } from "cloudflare:workers";
import { currentMember, ensureMemberSchema } from "@/lib/member-auth";
export async function GET() { await ensureMemberSchema(); const member = await currentMember(); return member ? Response.json({ member }) : Response.json({ error: "請先登入。" }, { status: 401 }); }
export async function PATCH(request: Request) { await ensureMemberSchema(); const member = await currentMember(); const { name, email } = await request.json(); const cleanEmail = String(email || "").trim().toLowerCase(); if (!member) return Response.json({ error: "請先登入。" }, { status: 401 }); if (!String(name || "").trim() || !/^\S+@\S+\.\S+$/.test(cleanEmail)) return Response.json({ error: "請填寫姓名與有效的 Email。" }, { status: 400 }); const existing = await env.DB.prepare("SELECT id FROM users WHERE email=? AND id<>?").bind(cleanEmail, member.id).first(); if (existing) return Response.json({ error: "此 Email 已被其他帳號使用。" }, { status: 409 }); await env.DB.prepare("UPDATE users SET name=?,email=? WHERE id=?").bind(String(name).trim(), cleanEmail, member.id).run(); return Response.json({ ok: true, email: cleanEmail }); }
